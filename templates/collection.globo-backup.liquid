
<div class= "uk-container">
<h2 class="uk-heading-medium uk-padding uk-padding-remove-bottom uk-padding-remove-left" style="">Fashion Forward,<br> Planet Friendly</h2>
<h5 class="uk-padding uk-padding-remove-top .uk-margin-remove-top uk-padding-remove-left" style="font-size: 22px; line-height: 1;">Stylish fashion, conscious choices, sustainable future</h5>

{% section "filter-collection-cloth" %}
<!-- {% section "collection-item-filters" %} -->

<!--       {% paginate collections.product by 3 %} -->
        <div class="uk-child-width-1-3@m uk-child-width-1-2@s uk-grid-small uk-grid-match" uk-grid>
              {% for collection in collections %}
                {% for product in collection.products limit:5 %}
                  
                    <div>
                      <div class="uk-card uk-card-default uk-card-hover uk-card-body">  
                      <a href="{{ product.url | within: collection }}" >
                          <img src="{{ product.featured_image.src | img_url: 'large' }}" alt="{{ product.featured_image.alt | escape }}" >
                      </a>
                      <h3 class="uk-card-title" style="color: #3B3B3B;"><a href="{{ product.url | within: collection }}">{{ product.title }}</a></h3>
                      <p>{{ product.price | money }}</p>   
                      </div>
                    </div>
                  
                {% endfor %}
              {% endfor %}
          </div>

<!--           <nav role="navigation">
      <ol class="pagination">
        {%- if paginate.previous-%}
          <li>
            <a href="{{ paginate.previous.url }}">
              <span aria-hidden="true">&laquo;</span>
              Previous <span class="visuallyhidden">page</span>
            </a>
          </li>
        {%- else -%}
          <li class="disabled">
            <span aria-hidden="true">&laquo;</span>
            Previous <span class="visuallyhidden">page</span>
          </li>
        {%- endif -%}

        {%- for part in paginate.parts -%}
          {%- if part.is_link -%}
            <li>
              <a href="{{ part.url }}">
                <span class="visuallyhidden">page</span> {{ part.title }}
              </a>
            </li>
          {%- else -%}
            {%- if part.title == paginate.current_page -%}
              <li class="active" aria-current="page">
                <span class="visuallyhidden">page</span> {{ part.title }}
              </li>
            {%- else -%}
              <li>
                <span class="visuallyhidden">page</span> {{ part.title }}
              </li>
            {%- endif -%}
          {%- endif -%}
        {%- endfor -%}

        {%- if paginate.next -%}
          <li>
            <a href="{{ paginate.next.url }}">
              Next <span class="visuallyhidden">page</span>
              <span aria-hidden="true">&raquo;</span>
            </a>
          </li>
        {%- else -%}
          <li class="disabled">
            Next <span class="visuallyhidden">page</span>
            <span aria-hidden="true">&raquo;</span>
          </li>
        {%- endif -%}
      </ol>
    </nav>
{% endpaginate %} -->


{% section "pagination" %}

</div>
<div class= "" style="width:100vw; height:413px; background: #8BB73F; margin-bottom:180px;">
  
  <img src = "{{ 'needle.svg' | asset_url }}"  style="position:absolute; right:0px; padding:30px">
  

<div class="" style="height: 100%; display: flex; align-items: center; justify-content: space-evenly;">
  <div class="uk-h2" style="color:#fff" style="">
      Sustainability is not just a trend,<br>it's a responsibility.
    </div>
    
        <div class="uk-h4 uk-text-center uk-margin-remove-top" style="color:#fff">
          "We believe in creating stylish pieces that not only <br> make you look good but also do good for the planet"
        </div>
    </div>
  </div>


</div>









